import { View } from "react-native"

export const Dvider:React.FC<{}> = ()=>{
    return(
        <View style={{alignSelf: "stretch", borderWidth: 0.5, marginHorizontal: 10, borderColor: 'gray'}} />
    )
}